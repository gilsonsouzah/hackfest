import React from 'react'

import { initializeApp } from '../config/firebase.config'

exports.

export default () => (
  <div style={{ color: `purple` }}>
    <Header></Header>
    <p>What a world.</p>
  </div>
)
